#!/usr/bin/env zsh
# ashlet.zsh - AI-powered shell auto-completion for Zsh
# Entry point: sources autocomplete/init.zsh which loads all components

# Resolve script directory and source the init module
source "${0:A:h}/autocomplete/init.zsh"
